# ============================================================
# requirements-production.txt
# Dependencias optimizadas para Render.com + Supabase + Upstash
# NO incluye: sentence-transformers, scikit-learn, elasticsearch,
#             minio, flower, opencv completo
# ============================================================

# Framework
fastapi==0.104.1
uvicorn[standard]==0.24.0
python-multipart==0.0.6

# Base de datos (Supabase = PostgreSQL)
SQLAlchemy==2.0.23
psycopg2-binary==2.9.9
alembic==1.12.1

# Seguridad
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
python-dotenv==1.0.0

# Validación
pydantic==2.5.0
pydantic-settings==2.1.0
email-validator==2.1.0

# OCR — versión headless (sin GUI, más liviano)
pytesseract==0.3.10
Pillow==10.1.0
PyMuPDF==1.23.8
pdf2image==1.16.3
opencv-python-headless==4.6.0.66
numpy==1.24.3

# Exportación
python-docx==1.1.0
openpyxl==3.1.2

# Utilidades
aiofiles==23.2.1
python-dateutil==2.8.2
httpx==0.27.0

# Redis — Upstash Redis en la nube
redis==5.0.1
hiredis==2.3.2

# Celery para tareas asíncronas
celery==5.3.4

# Búsqueda semántica
scikit-learn==1.3.0
sentence-transformers==2.2.2

# NLP Legal
spacy==3.7.2

# Corrección fuzzy de textos OCR (deduplicación de nombres con errores de escaneo)
rapidfuzz==3.6.1

# Rate limiting
slowapi==0.1.9

# Métricas (opcional, liviano)
prometheus-client==0.19.0

# Auditoría de seguridad de dependencias
pip-audit==2.7.3

# ============================================================
# EXCLUIDOS (instalar localmente si se necesitan):
#   sentence-transformers  — requiere torch (~2 GB build)
#   scikit-learn           — búsqueda semántica extendida
#   elasticsearch          — usar Postgres full-text en prod
#   minio                  — usar Supabase Storage o R2
#   flower                 — monitoreo Celery (no crítico)
#   opencv-python          — reemplazado por opencv-headless
# ============================================================
